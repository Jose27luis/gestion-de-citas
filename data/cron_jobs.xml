<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- Cron: Recordatorio 24 horas antes (diario a las 8:00 AM) -->
        <record id="cron_appointment_reminder_24h" model="ir.cron">
            <field name="name">Hospital: Recordatorio Citas 24h</field>
            <field name="model_id" ref="model_hospital_appointment"/>
            <field name="state">code</field>
            <field name="code">model._cron_send_reminders_24h()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="active" eval="True"/>
        </record>

        <!-- Cron: Recordatorio 2 horas antes (cada hora) -->
        <record id="cron_appointment_reminder_2h" model="ir.cron">
            <field name="name">Hospital: Recordatorio Citas 2h</field>
            <field name="model_id" ref="model_hospital_appointment"/>
            <field name="state">code</field>
            <field name="code">model._cron_send_reminders_2h()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">hours</field>
            <field name="active" eval="True"/>
        </record>

        <!-- Cron: Expiración de recetas (diario a la 1:00 AM) -->
        <record id="cron_prescription_expiry" model="ir.cron">
            <field name="name">Hospital: Expiración de Recetas</field>
            <field name="model_id" ref="model_hospital_prescription"/>
            <field name="state">code</field>
            <field name="code">model._cron_check_expiry()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="active" eval="True"/>
        </record>

        <!-- Cron: Cancelación automática de citas no confirmadas (cada hora) -->
        <record id="cron_appointment_auto_cancel" model="ir.cron">
            <field name="name">Hospital: Auto-Cancelar Citas No Confirmadas</field>
            <field name="model_id" ref="model_hospital_appointment"/>
            <field name="state">code</field>
            <field name="code">model._cron_auto_cancel_unconfirmed()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">hours</field>
            <field name="active" eval="True"/>
        </record>

    </data>
</odoo>
